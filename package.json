{
  "name": "flat-lang",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node build/index.js",
    "optimize": "node esbuild.config.mjs",
    "start": "npm run build && npm run optimize && npm run preview",
    "preview": "serve dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.9.1",
    "esbuild": "^0.24.0",
    "serve": "^14.2.4",
    "typescript": "^5.6.3"
  }
}
